server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Artem389/Microservices-Delivery-App.git
          default-label: main
          clone-on-start: true
          force-pull: true